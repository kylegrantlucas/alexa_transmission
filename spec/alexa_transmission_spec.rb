require 'spec_helper'

describe AlexaTransmission do
  it 'has a version number' do
    expect(AlexaTransmission::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
